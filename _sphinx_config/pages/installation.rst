============
Installation
============

```lmtest`` can be installed by cloning the repository or by downloading the lastest
distribution wheel and running::

.. code-block:: bash

  pip install lmtest-<version>-py3-none-any.whl
